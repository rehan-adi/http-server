{
  "name": "http-server",
  "version": "1.0.0",
  "description": "A simple HTTP server built from scratch using TypeScript.",
  "main": "./dist/server.js",
  "scripts": {
    "start": "node dist/server.js",
    "build": "tsc -p .",
    "dev": "nodemon --watch src --exec ts-node src/server.ts"
  },
  "keywords": [
    "http-server",
    "typescript",
    "node",
    "web-server",
    "backend"
  ],
  "author": "Rehan",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.10.5",
    "nodemon": "^3.1.9",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2"
  }
}
